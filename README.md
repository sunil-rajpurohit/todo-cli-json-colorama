# todo-cli-json-colorama
A Command Line Interface (CLI) based To-Do List App built with Python. Features include colored output using colorama, task persistence with JSON, and timestamps for every entry.
